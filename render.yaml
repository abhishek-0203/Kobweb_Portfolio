# Render Blueprint specification
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: kobweb-portfolio
    runtime: static
    buildCommand: |
      # Install Java 17
      curl -fsSL https://download.oracle.com/java/17/archive/jdk-17.0.12_linux-x64_bin.tar.gz -o jdk.tar.gz
      tar -xzf jdk.tar.gz
      export JAVA_HOME=$PWD/jdk-17.0.12
      export PATH=$JAVA_HOME/bin:$PATH
      java -version
      
      # Build the static site
      chmod +x gradlew
      ./gradlew :site:kobwebExport -PkobwebReuseServer=false -PkobwebEnv=prod -PkobwebExportLayout=STATIC --no-daemon --stacktrace
    staticPublishPath: site/build/kobweb/site
    headers:
      - path: /assets/*
        name: Cache-Control
        value: public, max-age=31536000, immutable
      - path: /*.js
        name: Cache-Control
        value: public, max-age=31536000, immutable
      - path: /*.css
        name: Cache-Control
        value: public, max-age=31536000, immutable
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
    routes:
      # Fallback for client-side routing (SPA behavior)
      - type: rewrite
        source: /*
        destination: /index.html
